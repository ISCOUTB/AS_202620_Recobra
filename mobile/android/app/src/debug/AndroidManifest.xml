<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- INTERNET en debug para hot reload y para llamar al backend local.
         No se habilita cleartext aquí: en emulador usar
         `adb reverse tcp:3000 tcp:3000` y base URL http://127.0.0.1:3000,
         o probar con Flutter web (chrome). -->
    <uses-permission android:name="android.permission.INTERNET"/>
</manifest>
